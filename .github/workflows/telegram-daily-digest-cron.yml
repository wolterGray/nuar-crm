name: Telegram daily digest cron (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Supabase cron retired
        run: |
          echo "Retired: Supabase Edge Function cron is disabled."
          echo "Telegram automation is controlled by the Hetzner backend and CRM settings."
